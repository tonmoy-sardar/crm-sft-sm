import { ThreeDecimalNumberDirective } from './three-decimal-number.directive';

describe('ThreeDecimalNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDecimalNumberDirective();
    expect(directive).toBeTruthy();
  });
});
